# Core Blueprints

Create project-level gameplay classes here.

Planned assets:

- `BP_SimRoomGameMode`

Notes:

- Point `DefaultEngine.ini` at `BP_SimRoomGameMode` after the asset exists.
- Keep global rules here; room mechanics should stay in interactable or puzzle-specific Blueprints.
