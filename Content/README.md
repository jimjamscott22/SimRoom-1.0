# Content Scaffold

This folder follows the Blueprint-first Unreal structure described in `docs/spec-sheet.md` and `docs/implementation-plan.md`.

Real gameplay assets should be created in Unreal Editor as `.uasset` files. The README files in this tree are placeholders that keep the folder structure in git and document the intended assets.

```text
Content/
  Blueprints/
  Maps/
  UI/
  Materials/
  Audio/
```
