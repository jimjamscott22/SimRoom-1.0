# UI Widgets

Planned widgets:

- `WBP_HUD`
- `WBP_MainMenu`
- `WBP_PauseMenu`
- `WBP_LogicInterface`

Implementation notes:

- `WBP_HUD` should handle the crosshair and interaction prompt first.
- Keep menus minimal until the button-door loop and Rooms 0-3 are stable.
- `WBP_LogicInterface` belongs to the visual programming room milestone.
